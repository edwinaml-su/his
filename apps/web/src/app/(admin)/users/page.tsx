/**
 * Gestión de usuarios y roles.
 * TODO(Sprint 2): listado, invitar, asignar roles por organización.
 * TDR §6.1, §6.2.
 */
export default function UsersPage() {
  return (
    <div className="space-y-4">
      <div>
        <h1 className="text-2xl font-bold">Usuarios y roles</h1>
        <p className="text-sm text-muted-foreground">
          TODO(Sprint 2): pantalla en construcción. Por ahora la asignación de
          roles se hace por seed/script.
        </p>
      </div>
    </div>
  );
}
