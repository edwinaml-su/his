{
  "name": "@his/bi",
  "version": "0.1.0",
  "private": true,
  "description": "HIS BI — Capa semantica Cube.dev + dbt snapshots (Beta.19b)",
  "//": "OWASP A03:2025 — los paquetes @cubejs-backend/* arrastran una cadena con vulnerabilidades HIGH/CRITICAL sin fix (decompress, extract-zip, request) en TODAS sus versiones publicadas. Este workspace es config-only: ningún código TS lo importa, ningún job de CI lo corre y no se despliega. Mantenerlos declarados metía esa cadena en el árbol de dependencias de todo el monorepo sin beneficio. Se instalan bajo demanda cuando arranque la fase BI — ver README §Cube.dev.",
  "scripts": {
    "cube:validate": "node -e \"require('./cube/cube.js'); console.log('cube.js OK');\"",
    "dbt:snapshot": "dbt snapshot --select dim_patient_snapshot",
    "dbt:debug": "dbt debug"
  },
  "devDependencies": {
    "@types/node": "^22.19.20"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
